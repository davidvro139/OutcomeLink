# OutcomeLink Build Plan

Working checklist toward the Phase 1 / MVP scope defined in the spec (§62), sequenced so each stage produces something runnable before the next begins. Check items off as they land; add dated notes inline if a decision changes.

## 0. Repo & tooling setup

- [x] Scaffold npm workspaces monorepo: `client/`, `server/`, `shared/` (see `docs/TECH_STACK.md`)
- [x] Root `tsconfig.base.json` + per-package `tsconfig.json`
- [x] ESLint + Prettier shared config
- [x] Husky + lint-staged pre-commit hook (lint + typecheck on staged files)
- [x] `.env.example` for `server/` (DB connection, JWT secret, etc.) — also added for `client/` (`VITE_API_URL`)
- [x] `shared/` package: role names, enrollment statuses, outcome classification codes, CPL metric enum (Completion/Placement/Licensure) — plus follow-up methods/outcomes and evidence types
- [x] Express app scaffold with health-check route, standard response/error envelope (spec §59), and the full module folder layout from spec §58 (this was originally listed under stage 2, but landed alongside the rest of the scaffold since it's tooling, not feature work)
- [x] Vite + React + TS client scaffold with Mantine/TanStack Query/React Router wired up and a connectivity-check dashboard page (originally stage 5 — same reasoning)

## 1. Database & Prisma

- [x] Install Prisma, point at local MySQL instance (MySQL 8.0 running as a local Windows service; dedicated `outcomelink` DB)
- [x] Translate `docs/DATA_MODEL.md` entities into `server/prisma/schema.prisma`, one entity group at a time (org structure → students → accreditation engine → outcomes → employers → follow-up/evidence → surveys → governance → operations) — 33 models, `prisma validate` and `prisma generate` both clean
- [x] First migration; confirm it applies cleanly to a fresh MySQL database — applied and verified via `SHOW TABLES`. Also added a follow-up migration for the `Evidence`/`VerificationRecord` "exactly one target" CHECK constraints flagged in §13; required setting `onDelete`/`onUpdate` to `Restrict` on those relations since MySQL forbids a CHECK on a column governed by a cascading FK action (Prisma's default for optional relations)
- [x] Seed script skeleton (empty for now — real synthetic data is step 8)

## 2. Backend foundation

- [x] Express + TypeScript app scaffold, module folders per spec §58 — done in stage 0
- [x] API conventions from `docs/TECH_STACK.md`/spec §59: response envelope, pagination params, standard error shape — done in stage 0
- [x] Zod request-validation middleware (`server/src/middleware/validate.ts`)
- [x] Auth: register/login, JWT access token + httpOnly refresh cookie, bcrypt password hashing (`server/src/modules/auth/`) — verified end-to-end against the real database (register, login, /me, /refresh, plus the wrong-password/duplicate-email/short-password error paths)
- [x] Role-based authorization middleware, including program-level and campus-level scoping (`UserProgramAccess`, `UserCampusAccess`) — `requireAuth`/`requireRole`/`requireProgramAccess`/`requireCampusAccess` in `server/src/middleware/auth.ts`, ready to apply once Program/Campus routes exist in stage 3
- [x] Generic audit-log service wired as a Prisma Client Extension (`server/src/lib/prisma.ts`) rather than a per-call service — automatically writes one `AuditLogEntry` row per changed field on every create/update/delete through the exported `prisma` client, attributing it to the request's user via `AsyncLocalStorage` (`server/src/lib/requestContext.ts`). Verified with a scripted update. Known scope boundary: only single-record create/update/delete are covered, not `*Many`/`upsert`/nested relation writes
- [x] Health-check endpoint + basic request logging (pino) — done in stage 0

**Checkpoint reached:** registered a user, logged in, and hit an authenticated `/api/auth/me` with a real bearer token against the live database. Role enforcement (`requireRole`) is implemented but has no protected route to exercise yet — that arrives with stage 3 CRUD.

**Follow-up (not yet done):** self-registration (`POST /api/auth/register`) is currently open to anyone and lets the caller pick any role, including `SYSTEM_ADMINISTRATOR` — fine for bootstrapping with no user-management UI yet, but must be locked down (admin-created users, or "first user in a new institution only") before this goes anywhere near production.

**Also fixed along the way:** `@types/express` had drifted to v5 (mismatched against the actual Express 4 runtime) both directly and via `@types/cookie-parser`/`@types/multer`'s own nested copies, causing `app.use()` overload errors. Pinned to v4 tree-wide via root `package.json` `overrides`.

## 3. Core CRUD (non-accreditation)

- [x] Institution / Campus / Department / Program (`server/src/modules/programs/`) — single-row Institution get/update, full Campus/Department/Program CRUD scoped to institution, nested Cohort under Program. Mutations restricted to System Administrator
- [x] Student, StudentCommunicationPreference, StudentEnrollment (`server/src/modules/students/`) — per-institution duplicate internal-ID detection (409), name/ID search, nested enrollment and communication-preference (consent/do-not-contact) endpoints
- [x] Employer, EmployerContact (`server/src/modules/employers/`) — search/filter, nested contacts
- [x] EmploymentRecord (`server/src/modules/placements/`, historical, never overwritten — spec §8) — a job change is a new record via create(), not a mutation of the old one; no delete endpoint, for the same reason
- [x] FollowUpAttempt + Follow-Up Queue endpoint (`server/src/modules/followups/`) — program/campus/staff/outcome/minAttempts/minDaysOverdue filters implemented; the spec's "reporting period" filter deferred until the outcomes/accreditation modules exist. Refuses to log an attempt against a do-not-contact student (409)
- [x] Evidence upload endpoint (`server/src/modules/evidence/`, multer + `server/src/lib/storage.ts`'s `StorageAdapter` interface, local disk for now) — enforces the "exactly one target" invariant (outcomeRecordId/employmentRecordId/licensureResultId) in application code ahead of the database CHECK constraint, with a download route that streams the file back after an institution-ownership check

**Checkpoint reached:** created a program, enrolled a student, logged employment, recorded a follow-up attempt (and verified the do-not-contact refusal), and uploaded + downloaded evidence — all through the API against the live database. Role enforcement verified (403 for a non-admin creating a program, 200 for listing).

**Follow-ups (not yet done):**
- List endpoints don't yet filter rows by program/campus access scope for program-/campus-scoped roles (e.g., a Program Administrator can currently list all programs, not just their assigned ones) — `requireProgramAccess`/`requireCampusAccess` exist and are ready to apply to single-resource routes, but list-query-level scoping needs its own filter logic.
- The Follow-Up Queue computes `daysOverdue`/`minAttempts` filtering in application code after a full institution-scoped fetch (documented in `queue.ts`) — fine at this project's data scale, but not a pattern to reuse for a larger dataset without revisiting.
- Evidence's `outcomeRecordId` and `licensureResultId` targets are validated for shape and institution ownership, but neither `StudentOutcomeRecord` nor `LicensureResult` has an owning CRUD module yet (accreditation engine and Phase 2's licensure workflow, respectively), so only the `employmentRecordId` path is exercised end-to-end so far.

## 4. Accreditation engine (do not skip the reading)

- [x] **Obtain and read official COE CPL documentation for the target reporting year before writing any rule logic** (spec §67) — read the current (Revised October 2025) Annual Report Help Manual plus the 2024 edition for worksheet detail not extractable as text in the current PDF; both cited in `docs/COE_RULE_MATRIX.md`. The Handbook of Accreditation itself is still unread — flagged as an open question there, to revisit before the Validation Engine/Improvement Plans work
- [x] Build the documented rule matrix: COE definition → classification → numerator behavior → denominator behavior → exclusion behavior → validation rules → automated tests — `docs/COE_RULE_MATRIX.md`. Caught and corrected a wrong guess already sitting in `docs/DATA_MODEL.md` §13 (continuing education is actually *related placement*, i.e. counts in the Placement numerator — not a Placement exclusion as an earlier draft assumed)
- [x] AccreditationFramework / RuleSet / ReportingPeriod CRUD (admin configuration) — `server/src/modules/accreditation/frameworks/`, `rules/`, `reportingPeriods.ts`. `RuleSet.ruleDefinition` stores only the benchmark percentages (60/70/70) as data; the classification/calculation algorithm itself is versioned, tested code (`classifiers/coe2026.ts`) rather than a generic rule-interpreter DSL — deferred per `docs/DATA_MODEL.md` §13 until a second framework actually needs one
- [x] Classifier interface + COE-2026 implementation, driven by the rule matrix — one classification per (student enrollment, reporting period, metric) per `docs/DATA_MODEL.md` — `server/src/modules/accreditation/classifiers/coe2026.ts`, pure and DB-free by design
- [x] Calculator interface for Completion / Placement / Licensure + unit tests — `server/src/modules/accreditation/calculators/cplCalculator.ts` orchestrates the classifier over every enrollment and aggregates per-program and institution-wide `CplCalculationResult` rows. 25 unit tests on the classifier itself (`coe2026.test.ts`), covering every classification branch and the two order-of-precedence rules (unavailable/refused checked before employment; already-employed-related checked before awaiting-licensure). Note: COE's own sample worksheet numbers in the Annual Report Help Manual couldn't be reliably reconstructed from the PDF text extraction (rows 12+ came out misaligned) — tests use self-constructed scenarios built directly from the verbatim rule definitions instead of risking wrong "official" numbers as ground truth
- [x] Explanation generator backing "How This Student Counts" (`CplCalculationExplanation`) — the classifier produces `reasonText` directly (it's the only thing that knows why), persisted 1:1 with each `StudentClassification`, including for `NOT_APPLICABLE` cases, so a student always has an answer for "why don't I count here"
- [x] Validation engine: ERROR/WARNING/INFORMATION checks from spec §22 — `server/src/modules/accreditation/validators/validationEngine.ts`. Implements missing-completion-date, missing-outcome-record, employment-without-employer, related-employment-without-justification, placement-without-verification, missing-evidence, missing-licensure-result, conflicting-withdrawal-and-employment, possible-duplicate-student (name-match), overlapping-reporting-period, and below-benchmark checks, plus list/resolve endpoints (`validation.ts`). **Not implemented**: "enrollment totals that do not reconcile" — COE's worksheet reconciles Beginning/New Enrollment counts against Still Enrolled + Completers + Withdrawals (docs/COE_RULE_MATRIX.md §2), but OutcomeLink tracks continuous per-student enrollments rather than period-bounded enrollment-count snapshots, so there's no honest way to run that specific check without first building an enrollment-ledger concept the schema doesn't have — documented in the module rather than faked
- [x] Reporting Period finalize/reopen workflow (spec §25) — finalize (OPEN/READY_FOR_REVIEW/REOPENED → FINALIZED), submit (→ SUBMITTED), reopen (mandatory reason, → REOPENED). Blocks recompute and outcome-record edits against a Finalized/Submitted period until explicitly reopened. Surfaced and fixed a real bug in the audit-log Prisma extension along the way: `typeof value === "object"` is true for `Date`, so every date-field change was silently skipped by the "skip nested relation writes" filter — fixed app-wide, not just here
- [x] Duplicate Student Resolution merge workflow (spec §23) — `server/src/modules/students/merge.ts`: `POST /api/students/:id/merge` reassigns every child record (enrollments, employment, licensure, follow-ups, communication events, surveys, validation issues) from the merged-away student onto the survivor inside a transaction, reconciles the 1:1 communication-preference row (do-not-contact carries over conservatively), and refuses to merge an already-merged student (409). The merged-away `Student` row is never deleted or mutated — `StudentMergeLog` is the authoritative, audited (who/when/why) record that it was merged, satisfying spec's "should remain retrievable... rather than deleted" directly

**Stage 4 complete.** All six items above are done, each verified against the live database (and several over HTTP), not just typechecked. This closes out the core accreditation engine — the "most important technical demonstration" per spec §65.

**Verified for this item:** created a duplicate ("Grace Hopper" re-entered under a different internal ID with her own follow-up attempt), merged it into the original, confirmed the follow-up attempt now belongs to the survivor, confirmed re-merging the same student is blocked (409), confirmed the merged-away row still exists untouched in the database, and confirmed the merge itself appears in `audit_log_entries` as a `StudentMergeLog` CREATE.

**Checkpoint reached:** verified end-to-end against the live database and over HTTP — created a framework/rule set/reporting period, ran nine hand-computed scenarios (covering every classification branch) through `computeReportingPeriod`, got exactly the expected numerator/denominator/percentage at both program and institution level, confirmed re-running is idempotent (no duplicate rows), then repeated a smaller version purely through the HTTP API (framework → rule set → reporting period → outcome record → compute → results → drill-down → per-student explanation) and got correct results there too. Separately verified finalize → blocked recompute/edit → submit → reopen (with and without a reason) → recompute works again, confirmed via direct SQL that every field change (including dates, post-fix) lands in `audit_log_entries`. Separately verified the validation engine against five seeded problem scenarios and got exactly the expected issue set, including a cross-period edge case and the duplicate-name detection; caught and fixed a false-positive (a 0/0 metric with no applicable students was being flagged as "below benchmark").

**Still open (low priority):** `computeReportingPeriod` always recomputes every enrollment from scratch (no incremental/partial recomputation) — acceptable at this project's data scale, revisit if that changes.

## 5. Frontend foundation

- [x] Vite + React + TS scaffold, Mantine provider/theme setup — done in stage 0
- [x] React Router layout shell with role-based route guarding — `client/src/auth/RequireAuth.tsx` (redirects to `/login`, preserving the intended destination) and `RequireRole.tsx` (renders an access-denied alert; not yet exercised by a real route since no role-gated pages exist until stage 6)
- [x] TanStack Query client + typed API client wrapper (reusing `shared/` types) — `apiClient.ts` now holds the in-memory access token (never localStorage) and attaches it automatically; `AuthProvider.tsx`/`AuthContext.ts` provide `user`/`status`/`login`/`logout` app-wide
- [x] Login page + silent token refresh handling — `LoginPage.tsx` (Mantine form, redirects if already authenticated); `AuthProvider` attempts `POST /api/auth/refresh` on mount using the httpOnly cookie, so a page reload restores the session without forcing a fresh login
- [x] App shell: nav, global search (spec §54) — `layout/AppLayout.tsx` (Mantine AppShell header with title, search, user menu/sign-out) + a real backend `GET /api/search` (`server/src/modules/search/`) searching Student/Employer/Program/EmployerContact within the caller's institution, wired to `components/GlobalSearch.tsx`'s debounced dropdown. No navbar yet — deliberately, since there are no feature pages to link to until stage 6

**Checkpoint reached — verified in a real browser**, not just typechecked: started both dev servers, drove headless Chromium (Playwright) through the full user-facing flow — logged out → redirected to `/login`; logged in → landed on the dashboard showing the real user's name; app shell header shows title, search, and user menu; typed a partial name into global search → got a real grouped dropdown from the live database; signed out → back to `/login`; logged in again and reloaded the page → silent refresh kept the session instead of bouncing to login. Screenshots taken at each step.

**Two real bugs the browser run caught that typecheck/lint didn't:**
1. `@outcomelink/shared` is CommonJS (kept that way so the server's Jest setup stays simple, per `docs/TECH_STACK.md`), but Vite's dev server serves linked npm-workspace packages as native ESM without routing them through esbuild's CJS-interop pre-bundling — so importing any named export (`ROLE_LABELS`, etc.) from it threw `does not provide an export named...` at runtime, with zero indication from `tsc`. Fixed via `optimizeDeps.include: ["@outcomelink/shared"]` in `client/vite.config.ts`.
2. The login page's email field never had `type="email"` set — cosmetically invisible (a Mantine `TextInput` looks identical either way) and harmless to the login flow itself, but it does mean no mobile "@"-keyboard and no native email format hinting. Fixed.

## 6. MVP feature screens

- [x] Program list/detail — `client/src/pages/programs/`: list with a create-program modal (campus select, System Administrator only), detail page with audit history
- [x] Student list/detail, enrollment, outcome record entry — `client/src/pages/students/`: search-as-you-type list; detail page with tabs for Enrollments (add enrollment, change status, add outcome records with the full COE-relevant field set), Employment, Follow-ups, and Audit History; do-not-contact toggle in the header
- [x] Employer CRM screens (list/detail, contacts) — `client/src/pages/employers/`: list with search/create, detail page with contacts table and add-contact form
- [x] Follow-up queue screen with filters and overdue highlighting — `client/src/pages/followups/FollowUpQueuePage.tsx`: minimum-days-overdue filter, overdue rows highlighted red with a day-count badge
- [x] Evidence upload/viewer on outcome and employment records — `client/src/components/EvidencePanel.tsx`, reused wherever evidence attaches; upload goes through `apiRequest`'s FormData path, viewing fetches the file as an authenticated blob (`apiRequestBlob`) rather than a plain link, since the download route requires a Bearer token a raw `<a href>` can't carry
- [x] CPL dashboard with full drill-down (metric → records → student → classification → evidence → audit history, spec §53) — `client/src/pages/accreditation/CplDashboardTab.tsx`: every non-N/A percentage is clickable into a numerator/denominator/excluded drill-down modal showing each student's classification and the classifier's own reasoning text
- [x] Program comparison view (spec §28, MVP-scoped subset) — folded into the same CPL Dashboard table rather than a separate screen: rows are programs plus an institution-wide summary row, columns are the three metrics, so program comparison and drill-down share one view instead of duplicating the same numbers in two places
- [x] Data validation / readiness screen (ERROR/WARNING/INFORMATION list, clickable) — `client/src/pages/accreditation/ValidationTab.tsx`: severity-badged issue list, clickable through to the student, with a Resolve action
- [x] Basic audit history view on a record — `client/src/components/AuditHistory.tsx`, reused on Program/Student/Employer/ReportingPeriod detail pages; needed a new backend endpoint (`GET /api/audit`) since only the automatic *write* side existed before this stage

**Checkpoint reached — verified in a real browser across the whole surface**, not just typechecked: two Playwright passes drove login → Programs (list, create, detail, audit history) → Students (list, detail, all four tabs) → Employers (list, detail, add contact) → Follow-Up Queue → Accreditation (reporting period detail, CPL dashboard, drill-down modal with real numerator/denominator/excluded data and reasoning text, Data Validation tab showing the real issues from earlier backend testing) → Audit History. Screenshots taken throughout; final passes showed zero console errors.

**Three real bugs the browser runs caught that tsc/eslint didn't:**
1. Several create/edit forms rendered a field via `getInputProps(...)` that wasn't declared in the form's `initialValues` (email, industry/city/state, notes, several outcome-record Select fields) — React warned about an input silently flipping from uncontrolled to controlled the moment a value was set. Fixed by declaring every bound field in `initialValues`; for Mantine `Select`s specifically, that meant `null` rather than `undefined`, since `undefined` is what triggers the uncontrolled state in the first place.
2. Fixing bug #1 by defaulting `email` to `""` created a second bug: Mantine always sends every declared field on submit, so a blank email now sent literally `email: ""` to the server — which fails Zod's `.email()` format check even though the field is optional (`undefined` would have passed). Added `client/src/lib/forms.ts`'s `stripEmptyStrings()` and applied it at both call sites (student creation, employer contact creation) — the general lesson being that "optional" and "empty string" are not the same thing to a format validator, and a form library that always includes every field papers over that difference.
3. The evidence "view" action almost shipped as a plain link/URL to the authenticated download route — which would silently fail (no way to attach a Bearer token) or, worse, appear to work in a manual click-test while actually failing for any viewer without a live session cookie. Caught before shipping by re-reading the backend route's `requireAuth` middleware while writing the frontend call; fixed with `apiRequestBlob()` fetching the file with the header attached and handing the browser a `blob:` URL instead.

## 7. Testing

- [x] Jest + Supertest set up in `server/`, first tests on auth + one CRUD module — `jest.config.cjs` split into `unit`/`integration` projects; integration tests run against a real dedicated `outcomelink_test` MySQL database (`jest.globalSetup.cjs` runs `prisma migrate reset` and hard-guards the DB name so a misconfigured `DATABASE_URL` can't ever reset a real environment). `auth.integration.test.ts` (9 tests: register, login, /me, /refresh) and `programs.integration.test.ts` (8 tests, including cross-institution 404 isolation) added. 25 pre-existing classifier unit tests + these 17 integration tests all passing (`npm test`, `npm run test:integration`).
- [x] Vitest + React Testing Library set up in `client/` — shares `vite.config.ts` via `defineConfig` from `vitest/config`; `client/src/test/setup.ts` polyfills `window.matchMedia` (jsdom doesn't implement it, and Mantine's `MantineProvider` calls it on mount — a real failure only the running test caught) and `renderWithProviders.tsx` wraps `MantineProvider` + a fresh `QueryClient`. Bumped `vitest` from the initially-installed `^2.1.8` to `^5.0.0` after `npm audit` flagged known vulnerabilities in 2.x (0 vulnerabilities after). `StudentExplanationPanel.test.tsx` — 2 tests, including that a `NOT_APPLICABLE` classification still renders its own reasoning text. `npm run -w client test` passing.
- [x] Cypress set up with one smoke E2E flow (login → create student → view CPL dashboard) — `client/cypress/e2e/smoke.cy.ts`, run headless against the real dev servers (`cypress.config.ts`, `baseUrl http://localhost:5173`). Hit and resolved two real environment bugs along the way, not just config: (1) Cypress 16.0.0's Windows binary is broken (`Cypress.exe: bad option: --smoke-test`) independent of this project — downgraded to the well-established `15.21.1`; (2) that same error persisted even on 15.21.1 because this shell's `ELECTRON_RUN_AS_NODE=1` (set by the VS Code integrated terminal/extension host) forces every Electron binary launched from it — Cypress's own included — to run as plain Node, whose CLI rejects unrecognized flags with that exact "bad option" / exit-9 signature; confirmed by `Cypress.exe --version` printing a Node version string instead of Cypress's. Fixed at the root rather than papered over: `client/scripts/run-cypress.cjs` deletes that env var before spawning Cypress, and `npm run e2e` / `npm run cypress:open` go through it, so the fix travels with the repo instead of depending on whoever runs it remembering a manual workaround. `npm run e2e` now passes (1/1) in ~5s.
- [x] Accreditation engine test matrix built directly from the rule matrix (spec §55) — `coe2026.test.ts` (25 unit tests, every classification branch: employment status precedence, unavailable/refused-before-employment-status ordering, continuing-education-and-military counted as related placement per `docs/COE_RULE_MATRIX.md`, awaiting-licensure vs. already-employed-related ordering) plus `cplCalculator.integration.test.ts` (7 tests against the real test database: Completion 7/8, Placement 4/5, Licensure 1/1, institution-wide rollup matches per-program sums, `determinedByRuleSetId` recorded on every classification, `NOT_APPLICABLE` persisted with a real explanation rather than a gap, idempotent recompute with no duplicate rows). Historical rule-set versioning is exercised structurally (every classification/result row is tied to the `RuleSet` that produced it) but only one rule set (`coe2026`) exists yet, so a second historical rule set isn't covered until one is actually built.

## 8. Demo data

- [x] Synthetic data generator (`@faker-js/faker`) for Mountain West Technical College (Main + North campus) — `server/prisma/seed.ts`, run via `npm run prisma:seed`. Deterministic (`faker.seed(...)`) so re-seeding a fresh database reproduces the same dataset. Wipes every table first (same "reset before reseed" pattern as `jest.globalSetup.cjs`), then builds the institution/campuses/departments, 14 programs across 4 departments with per-program target completion/placement/licensure rates, 11 staff users (including `ada@mwtc.edu` / `password123`, required by the frontend's dev-only Demo Login button), 130 employers with contacts, then students/enrollments/outcomes/employment/licensure/evidence/follow-ups driven by each program's target rates, and finally calls the real `computeReportingPeriod()` + `runValidation()` for every period before finalizing the historical ones — so the seeded CPL results and validation issues are the actual engine's output, not pre-baked numbers.
- [x] Seed 1,000+ students, 100+ employers, 3–5 reporting years — 1,139 students, 130 employers, 4 reporting periods (2023/2024 Submitted, 2025 Finalized, 2026 Open/current).
- [x] Deliberately seed edge cases: programs below benchmark, missing outcomes, missing verification, licensure failures, strong/weak programs, unknown outcomes, duplicate-student candidates — confirmed present by querying the seeded database directly: `BELOW_BENCHMARK_COMPLETION`/`_PLACEMENT`/`_LICENSURE` (29 total, including the three programs deliberately configured below benchmark — Plumbing Technology, Surgical Technology, Barbering — plus incidental ones from natural randomness, which is realistic), `MISSING_OUTCOME_RECORD` (49), `MISSING_COMPLETION_DATE` (32), `MISSING_LICENSURE_RESULT` (10), `PLACEMENT_WITHOUT_VERIFICATION` (122), `MISSING_EVIDENCE` (276), `POSSIBLE_DUPLICATE_STUDENT` (72, including 8 deliberately injected same-name pairs), `CONFLICTING_WITHDRAWAL_AND_EMPLOYMENT` (213). Surgical Technology is also a deliberate "program didn't exist yet" case (`startYear: 2024`), exercising the zero-denominator-isn't-a-failure rule on the two earlier periods.

**Checkpoint reached — verified in a real browser against the full seeded dataset**, not just queried from the database: Playwright confirmed login, all 14 programs, real student names/`MWTC-XXXXXX` IDs, a student's "How This Student Counts" panel showing real classifications and reasoning text, 130 employers, a populated Follow-Up Queue, all 4 reporting periods with correct statuses, the 2025 CPL Dashboard's percentages and below-benchmark badges, a working drill-down modal, and the Validation tab's full mix of severities and issue types.

**Two real frontend bugs the browser run caught that only showed up at this data scale** (not present in earlier, smaller-scale manual testing): the Students and Employers list pages had no pagination UI at all — `useStudents`/`useEmployers` hardcoded `pageSize: 50` with no way to reach the rest of 1,139 students or see a total count. Fixed by adding a `page` parameter to both hooks and a total-count line + Mantine `Pagination` control to both pages. Separately, the Programs list had no Campus column despite campus being a required, meaningful field — fixed by looking up the campus name from the already-fetched `useCampuses()` data.

---

## 9. Accreditation rule corrections & fields (from `docs/Outcomes and CPL slides.pdf`)

An institution's internal outcomes-training deck (2017, COE member college) was reviewed against `docs/COE_RULE_MATRIX.md`. Most of it confirms the current classifier is already correct (military/National Guard and continuing-education-at-any-institution both counting as related placement, the three "refused employment" sub-reasons, awaiting-licensure/unavailable exclusions). These items are the real, actionable gaps it surfaced:

- [x] **Enrollment Objective scoping** — official-source text-extraction was genuinely blocked (see git history for the earlier note on that attempt), but resolved directly by someone with institutional COE experience: enrollment objective does gate CPL scope, and there's a third category beyond the deck's two — **Secondary** (dual-enrolled high-school students) — also excluded. Confirmed these labels ("Certificate Seeker"/"Occupational Upgrade"/"Secondary") are that institution's own SIS vocabulary for a real COE-relevant distinction, not COE-mandated terminology, so it's modeled as configurable rather than a fixed enum — see `COE_RULE_MATRIX.md` §2's "Enrollment Objective scoping" note for the full resolution. `StudentEnrollment.enrollmentObjective` (free text) + `StudentEnrollment.reportableForAccreditation` (`Boolean`, default `true` — the actual gate) plus a new `NOT_REPORTABLE` completion classification, checked before every other rule in `coe2026.ts`. Frontend: both fields on the create-enrollment form, plus an editable "Reportable for accreditation" checkbox on each existing enrollment. Verified: new classifier unit test and a new `cplCalculator.integration.test.ts` case (a reportable=false graduate completer with related employment doesn't shift the program's 7/8 completion or 4/5 placement ratios) — 58/58 server tests passing. Seed data updated (~5% of enrollments are Secondary/Personal-Enrichment and excluded; 216 real `NOT_REPORTABLE` classifications produced against the demo dataset).
- [x] **Implement the enrollment-stage "allowable subtraction" categories** that `COE_RULE_MATRIX.md` §2 already documents from the official Help Manual (documented unavailability — pregnancy/health/incarceration/death, church-mission/foreign-aid/military-activation, 100%-refund/first-day-only) but `coe2026.ts`'s classifier never actually checked. `StudentEnrollment.allowableSubtractionReason` (new `AllowableSubtractionReason` enum, meaningful only when `enrollmentStatus` is `WITHDRAWN`) plus a new `ALLOWABLE_SUBTRACTION` completion classification (counted in neither numerator nor denominator) in `coe2026.ts`, wired through `cplCalculator.ts`. "Transferred internally" is handled for free by the existing `TRANSFERRED` enrollment status; "secondary students" ended up handled by the Enrollment Objective item above instead of this enum, once that was resolved. Frontend: `EnrollmentsTab.tsx` shows a reason picker only when status is Withdrawn, clearable. Verified: new classifier unit test (a withdrawal with a documented reason produces `ALLOWABLE_SUBTRACTION`, not `WITHDRAWAL`) and a new `cplCalculator.integration.test.ts` case (confirms it doesn't shift the program's 7/8 completion ratio) — 56/56 server tests passing. Seed data updated so ~18% of withdrawals now carry a reason (53 real `ALLOWABLE_SUBTRACTION` classifications produced against the demo dataset).
- [x] **Outcome relatedness provenance** — `StudentOutcomeRecord.relatedToTrainingSource` (`RelatedToTrainingSource`: `STUDENT_REPORTED` / `INSTRUCTOR_REPORTED`), alongside the existing `relatedToTrainingJustification` field. Provenance only, doesn't affect classification. Frontend: a "Who determined relatedness?" Select appears in the outcome-record form only when "Related to training" is checked.
- [x] **Verifiable-employer-contact validation check** — new `EMPLOYER_MISSING_VERIFIABLE_CONTACT` (WARNING) in `validationEngine.ts`: flags an `EMPLOYED` outcome whose linked `Employer` has neither an address/city nor any contact phone number on file. Covered by a new `validationEngine.integration.test.ts` (3 tests: flags a fully-unverifiable employer, doesn't flag one with an address, doesn't flag one with only a contact phone). Seed data updated so ~5% of employers are deliberately unverifiable (22 real issues produced against the demo dataset).

**Checkpoint reached — verified in a real browser, not just tested against the database.** Two real bugs the browser run caught (backend tests alone didn't, and couldn't):
1. Selecting "Withdrawn" crashed the whole app (`Cannot read properties of undefined (reading 'map')`, React tree unmounted to a blank page) — not a code defect but a stale Vite `optimizeDeps` cache: the client dev server had pre-bundled an older build of `@outcomelink/shared` before the new exports (`ALLOWABLE_SUBTRACTION_REASONS`, etc.) existed, and doesn't auto-detect a linked workspace package's dist output changing underneath it. Fixed by clearing `client/node_modules/.vite` and restarting the dev server. This hit again during Phase 2 P1 (`LICENSURE_RESULT_STATUSES` undefined, same crash signature) — costly enough across three separate incidents that it's now fixed at the root instead of just documented: `client/package.json` has a `predev` script that clears `node_modules/.vite` automatically before every `npm run dev`, so a stale pre-bundle can't survive a dev-server restart regardless of whether anyone remembers this note.
2. Both new Selects had Mantine's default `allowDeselect={true}`, so re-clicking an already-selected option silently cleared it back to `null` with no confirmation — sent a real `PATCH ... allowableSubtractionReason: null` on re-click. Consequential for the subtraction-reason field specifically, since an accidental clear reverts a withdrawal from "excluded, neutral" back to "counts against the institution." Fixed with `allowDeselect={false}` on both; re-verified the fix holds (re-clicking now leaves the value unchanged, no stray request fires).

The Enrollment Objective work above (new-enrollment form's objective text input + reportable checkbox defaulting checked, and the editable checkbox on existing enrollments) was separately verified in a real browser with no new bugs: created a "Secondary"/unreportable enrollment, confirmed it displayed and persisted correctly, toggled it back reportable, and confirmed a pre-existing seeded enrollment showed its real "Certificate Seeker" objective. Clean console throughout.

## Phase 2 (after MVP checkpoint above)

Per spec §63. Broken into stages the same way the MVP was, ordered roughly by dependency and value. `server/src/modules/{imports,licensure,notifications,reports,surveys}` already exist as empty placeholder scaffolds (`export {};`) from early setup — noted per stage below since it's easy to assume something's further along than it is just because the folder exists.

- [x] **P1. Licensure workflow & dashboard** — previously `LicensureResult` rows only existed via `seed.ts`; there was no way for a real user to record an exam attempt/result. Backend: real routes in the previously-empty `licensure` module — `GET/POST /api/students/:studentId/licensure-results` (auto-assigns `attemptNumber`), `PATCH .../:id`, and `GET /api/licensure/queue` (every graduate completer of a licensure-required program without a resolved PASSED/FAILED result yet — a work-queue distinct from the CPL Dashboard's existing aggregate pass-rate percentages, which already covered the "dashboard" half of this item via the LICENSURE metric column + drill-down). Frontend: a "Licensure" tab on the student detail page (record attempts, inline result editing, Program select filtered to licensure-required programs only) and a new "Licensure Queue" nav item/page. Covered by 9 new integration tests (including that the queue is scoped per student+program — an unrelated program's result doesn't clear a student's real outstanding entry) — 67/67 server tests passing. Verified in a real browser end-to-end with no remaining bugs after fixing the dev-server restart issue below.
- [x] **P2. Accreditation readiness dashboard** — a rollup view: per program × reporting period, which of the 3 benchmarks are met, negotiated-rate overrides applied, open validation issue counts. (Days-until-outcomes-deadline deferred until the outcomes-follow-up-deadline item near the bottom of this list lands — there's no deadline concept to count down to yet.) Building this exposed a real, previously-unmodeled gap `COE_RULE_MATRIX.md` had already flagged: negotiated benchmarks (a Commission-approved alternate rate for one program+metric, e.g. a court reporting program's completion rate) didn't exist anywhere — the standard 60/70/70 institution-wide benchmarks were the only ones ever compared against, in both the Validation tab and anywhere else. New `NegotiatedBenchmark` model + `getEffectiveBenchmark()` helper (`server/src/modules/accreditation/benchmarks/negotiatedBenchmarks.ts`) is now the single source of truth both `validationEngine.ts`'s below-benchmark check and the new readiness endpoint call, so the two can never disagree about whether a program is meeting its real target. Backend: `GET/POST /api/programs/:programId/negotiated-benchmarks` (System Administrator only, matching program-management's existing role gate) and `GET /api/accreditation/reporting-periods/:id/readiness`. Frontend: a "Readiness" tab on the reporting period detail page (summary cards + per-program metric badges, purple "negotiated X%" badge when an override applies) and a "Negotiated Benchmarks" section on the program detail page. 7 new integration tests (including the full round-trip: a program below the standard benchmark flags in Validation and shows Not Ready, then an added negotiated rate it meets clears both) — 74/74 server tests passing. Verified in a real browser: correct data, and confirmed the System-Administrator-only "Record Negotiated Rate" button is correctly hidden for an Institutional Administrator account.
- [x] **P3. Historical trend reporting** — CPL metrics charted across all reporting periods per program/institution-wide, using `@mantine/charts` (already a dependency, unused until now). Backend: `GET /api/accreditation/trends?programId=X` (omit for institution-wide) — chronologically ordered per-period results, reusing `CplCalculationResult` directly rather than recomputing anything. Frontend: a new "Trends" nav item/page with a Program select and a `LineChart` (3 series: Completion/Placement/Licensure, 0–100% y-axis). 3 new integration tests, including a genuine improving-trend fixture (25% → 75% completion across two periods) to confirm periods come back in order with the right numbers attached to each, not just "some data" — 77/77 server tests passing. Verified in a real browser: correct institution-wide and per-program values via tooltip inspection, chart re-renders correctly when switching programs and clearing back to institution-wide, no console errors.
- [x] **P4. Improvement plans** — `ImprovementPlan`/`ImprovementPlanUpdate` were fully modeled but had no backend routes or UI at all. Also added a minimal `GET /api/users` staff directory (didn't exist anywhere — nothing could populate a "responsible person" picker before this). Backend: full CRUD (`GET/POST /api/accreditation/improvement-plans`, `GET/PATCH .../:id`, `POST .../:id/updates`), gated to operational roles the same way `outcomes.ts` is. Frontend: an "Improvement Plans" tab (create, status transitions, progress-update log) on the reporting period detail page, plus a "+" quick-create shortcut directly on the CPL Dashboard's below-benchmark cells — linked to both surfaces as the item asked, not just one. Building the dashboard shortcut surfaced a real, pre-existing inconsistency: the CPL Dashboard hardcoded the standard 60/70/70 benchmarks and never checked P2's negotiated-rate overrides, so a program with an approved rate it met could show "Below benchmark" there while the Readiness tab correctly showed it as ready — fixed by having the dashboard consume the same Readiness endpoint data instead of its own hardcoded copy. 6 new integration tests — 86/86 server tests passing. Two real bugs caught by browser verification (not by typecheck/tests): the classic "field missing from `useForm` `initialValues`" uncontrolled-input warning (`currentResult`/`target`/`dueDate` weren't declared on either the New Plan or quick-create forms) and a "target ?%" display artifact when a target was left blank — both fixed and re-verified clean.
- [x] **P5. Employer relationship analytics** — top employers by placement count, employer concentration risk (spec's "Employer Concentration/Pipeline" drill-down), industry breakdown. Backend: `GET /api/employers/analytics?reportingPeriodId=X` (institution-wide, all-time when omitted) — scoped to every `EMPLOYED` outcome record with an employer on file (not just the narrower COE "related placement" numerator, since this is about the real relationship), returning top employers, an industry breakdown, and a concentration summary (top-1 and top-5 share of total placements, with a LOW/MODERATE/HIGH risk label at 10%/25% thresholds). Frontend: a "View Analytics" entry point from the Employers list (not a new top-level nav item — kept as a secondary view reachable from where employers already live, same information-architecture call as Negotiated Benchmarks living on the Program detail page rather than getting its own nav slot) with summary cards, a top-employers table, and a `@mantine/charts` `PieChart` industry breakdown. 4 new integration tests, including a deliberately concentrated fixture (one employer takes 60% of placements) to confirm the concentration math and HIGH-risk classification are actually computed correctly, not just "some data" — 90/90 server tests passing. Verified in a real browser against the full demo dataset (765 all-time placements, 129 distinct employers, correctly re-filtering by reporting period) with no bugs found.
- [x] **P6. Administrative dashboards** (Executive, Program Health, Data Quality) — role-based landing dashboards replacing the placeholder `DashboardPage` ("Stage 5 checkpoint..." filler text left over from early development). Deliberately built as a composition of endpoints that already exist elsewhere (Readiness, Trends, Employer Analytics, both queues, Validation issues) rather than new aggregation logic, so a dashboard number can never drift out of sync with the page that number actually lives on — Program Health specifically just reuses `ReadinessTab` directly rather than re-implementing it. Executive adds one new bit of computation (a period-over-period trend delta per metric, from the existing Trends data) and is gated to System/Institutional Administrator; Data Quality (period-scoped severity/issue-type breakdown of open `ValidationIssue` rows, plus a bar chart) is gated the same way; Program Health is visible to every role. Verified in a real browser with two different logins: an Institutional Administrator sees all three tabs with real numbers throughout (including the trend arrows and a period-switch on Data Quality correctly updating every count), and an Instructor-role login correctly sees only Program Health — confirming the role gate actually works, not just that it compiles.
- [x] **P7. Excel exports** — CPL results, validation issues, and student lists as downloadable `.xlsx`, using `exceljs` (new dependency) behind one shared `sendXlsx()` helper (`server/src/lib/xlsx.ts`) so every export gets consistent formatting rather than reimplementing response headers/streaming three times. Frontend: a `downloadFile()` helper (same "fetch with the Authorization header, then hand the browser a blob" reasoning as the existing evidence-file viewer, but triggering a save instead of opening inline) backing three "Export to Excel" buttons — CPL Dashboard tab, Data Validation tab, Students list page. Installing `exceljs` surfaced a moderate `npm audit` finding in its transitive `uuid` dependency (a buffer-bounds-check issue in `uuid`'s v3/v5/v6 functions) — traced to confirm it's not actually reachable (`exceljs` only ever calls `uuid.v4()`, a different function entirely unaffected by that advisory) before deciding whether to act on it; fixed anyway via the root `overrides` block (same mechanism already used for `@types/express`), bumping to `uuid@^11.1.1` — required the same full `node_modules` wipe + reinstall the `@types/express` override needed originally, `npm audit` clean afterward. 4 new integration tests that parse the returned `.xlsx` back with `exceljs` and assert on real cell values, not just response headers — 94/94 server tests passing. Verified in a real browser with actual downloaded-file inspection (not just triggering the download): all three files opened as valid, well-formed workbooks with real data — 46 CPL result rows, 277 validation-issue rows, and exactly 1,196 student rows matching the seeded institution count.
- [x] **P8. Advanced drill-down reports** — Time-to-Employment, Placement Quality, Outcome Funnel, Unknown Outcome, Follow-Up Effectiveness (Employer Concentration/Pipeline already folded into P5). Backend: five new `GET /api/reports/*` endpoints in the previously-empty `reports` module. Time-to-Employment buckets days-to-placement (0–30/31–60/61–90/90+) per program and overall, from completion date to employment start date. Placement Quality is deliberately institution-wide, not per-program — documented in-code that `EmploymentRecord` carries no `programId`/`reportingPeriodId` of its own, so a per-program split would mean guessing which enrollment a placement belongs to, the same documented-scope-limit standard `validationEngine.ts` already holds itself to. Outcome Funnel measures data-completeness (Completers → Employed → Employed Related → Verified → Evidence on File) from the raw outcome-record fields, a deliberately different lens from the CPL classifier's derived PLACEMENT judgment — it's answering "where does our documentation trail break down," not re-deriving the accreditation number. Unknown Outcomes aggregates the existing SEEKING_OR_UNKNOWN classification and MISSING_OUTCOME_RECORD validation issue by program, surfacing individual students by name. Follow-Up Effectiveness is deliberately *not* period-scoped — a follow-up campaign for one period's completers routinely runs past that period's end date, and "is staff follow-up work paying off" is a cross-period question anyway — it defines a `RESOLVING_OUTCOMES` set of follow-up outcomes that actually resolved contact status, excluding ones like `NO_RESPONSE`/`FOLLOW_UP_REQUIRED` that just mean "still pending." Frontend: a new "Reports" tab on the reporting period detail page with a vertical 5-way sub-tab layout (stat cards, `@mantine/charts` bar/pie charts, and drill-down tables per report), reusing the same Paper/Table/chart idioms as the P6 dashboards. 6 new integration tests against one deliberately hand-computed fixture (four completers spanning every data-completeness level, an independent follow-up-attempt fixture) asserting exact numbers throughout, not just "some data came back" — 100/100 server tests passing. Verified in a real browser against the full demo dataset across two reporting periods (2023 SUBMITTED and 2026 FINALIZED) with correct, differing numbers per period (e.g. 204 vs. 113 Time-to-Employment placements) confirming data refetches rather than going stale, a working drill-down link from Unknown Outcomes into a student's detail page, and Follow-Up Effectiveness correctly showing identical totals regardless of which period's tab it's viewed from. One real gap found and fixed: the Follow-Up Effectiveness pie chart had no way to tell which color mapped to which outcome type without hovering every slice — Mantine's `PieChart` in this version has no built-in `withLegend` option, so a small manual color-swatch legend was added next to the chart.
- [x] **P9. Survey system** (Graduate + Employer) — `GraduateSurvey(Response)`/`EmployerSurvey(Response)` were modeled but the `surveys` module was an empty placeholder; no send/response flow existed yet. Since a graduate or employer filling out a survey has no OutcomeLink account, access had to be link-based rather than session-based — added a `responseToken` (opaque, `randomUUID()`-generated) column to both `GraduateSurvey` and `EmployerSurvey` via a hand-authored migration (this environment can't run `prisma migrate dev` interactively; wrote `migration.sql` directly and applied it with `migrate deploy`, verifying first that both tables were empty so a `NOT NULL UNIQUE` column could be added directly) so a public link can't be used to enumerate other students' surveys by walking sequential ids. Backend: staff-authenticated `GET/POST /api/students/:studentId/{graduate,employer-surveys}` (sending an employer survey requires an actual `EmploymentRecord` already on file for that student+employer, not just any employer in the system) plus fully public, unauthenticated `GET/POST /api/public/surveys/{graduate,employer}/:token` routes for the respondent's own link — a second submission attempt against an already-answered survey correctly 409s rather than silently overwriting or crashing. Per spec §43/docs/DATA_MODEL.md §9, survey responses are self-reported and display-only by design — they never auto-write to `StudentOutcomeRecord`/`EmploymentRecord`; a staff member reviewing a response still has to go enter/verify the real outcome data themselves on the existing tabs, so no "promote to verified data" action was built. Frontend: a "Surveys" tab on the student detail page (send + list + expandable response detail, "Copy Link" since there's no real email/SMS delivery infrastructure yet — same gap as everywhere else in the app that would need `notifications`, still an empty placeholder), plus two standalone public pages (`/survey/graduate/:token`, `/survey/employer/:token`) outside the authenticated app shell entirely, with a Mantine `Rating` component for the 1–5 scale questions. 16 new integration tests (send/list/public-fetch/submit/duplicate-submit/role-gating for both survey types) — 116/116 server tests passing. Verified in a real browser end-to-end, including the anonymous half: sent a graduate survey, opened the copied link in a fresh unauthenticated context, submitted it, confirmed the staff-side tab flipped to "Responded" with the exact submitted data, confirmed re-submitting the same link correctly 409s at the API level (not just a client-side guard), and confirmed a garbage token shows a clean "not found" state. Employer survey flow verified the same way, including that the employer picker only lists employers with a real employment record for that student. One real bug found and fixed: the "already responded" state reused the identical green "Thank you!" box as a fresh successful submission, differing only in body text — a viewer skimming could mistake "already done" for "just succeeded"; changed to a visually distinct blue "Already completed" state.
- [x] **P10. Duplicate student merge UI** — `server/src/modules/students/merge.ts` and `StudentMergeLog` already worked from the MVP (turned out to have zero test coverage despite this list previously saying "built + tested" — corrected by adding real integration tests alongside this stage's work), but nothing in the UI surfaced a "Merge" action from a `POSSIBLE_DUPLICATE_STUDENT` validation issue. A `ValidationIssue` row only ever carried the ONE flagged student's id, not which other student it's a duplicate of, so a new `GET /api/students/:id/duplicate-candidates` endpoint was added, reusing the exact same normalized-name match rule `validationEngine.ts` uses to raise the issue in the first place (case/whitespace-insensitive first+last name, same institution, excluding anyone already merged away) — the candidate list can never disagree with the issue that sent staff there. Also added a safety check `merge()` was missing: rejecting a merge INTO a student who has themselves already been merged away (previously only the reverse — merging an already-merged student — was checked), a scenario that was unreachable with no UI trigger but became reachable the moment a real "Merge" button existed. Frontend: an orange "Merge" button appears only on `POSSIBLE_DUPLICATE_STUDENT` rows in the Data Validation tab, opening a modal to pick the matching candidate, compare internal ID/email side by side, choose which record survives, give a required reason, and merge — on success the triggering issue is auto-resolved so it disappears from the list immediately rather than waiting for the next "Run Validation" click. Investigating this also surfaced a real, more serious latent bug: `validationEngine.ts`'s duplicate-name scan queried every student row on every run with no exclusion for already-merged-away students (who are never deleted, only reassigned away from), so a merged pair would have been re-flagged as duplicates forever — fixed by excluding `StudentMergeLog.mergedStudentId`s from that scan, the same exclusion the new candidates endpoint uses. 10 new integration tests (candidate matching/exclusions, both merge safety checks, child-record reassignment, and specifically that a merged pair stops being flagged on a later validation run) plus 3 more for an unrelated bug noticed during browser verification — student search (`GET /api/students`, and the header's global search) compared the whole query string against a single column, so a full "First Last" search matched nothing even though single-token searches worked; fixed by splitting on whitespace and requiring each token to match some field — 129/129 server tests passing. Verified in a real browser against the demo dataset's deliberately-seeded duplicate candidates (16 open `POSSIBLE DUPLICATE STUDENT` issues, all and only those showing the Merge button): completed two real merges end-to-end, confirmed the reciprocal issue for an already-merged pair cleanly shows "no other student record currently matches" instead of erroring, and confirmed the merged-away student's detail page still renders correctly across all tabs. No bugs found in the merge flow itself.
- [x] **P11. Bulk verification/follow-up actions** — act on multiple rows at once instead of one at a time. Scoped to two concrete actions rather than a generic "bulk verify outcome records" feature: there was no existing single-record "verify" action anywhere in the app to bulk-ify (`verificationStatus` is just a plain field on the create/update outcome-record form, not its own action), and building one from scratch would have been a separate, much larger feature than "add bulk to what exists." The two actions chosen both had a real single-record predecessor already: **Bulk follow-up logging** on the Follow-Up Queue (log one attempt — method/outcome/date/notes — against many selected students at once, e.g. "called this whole list today, no answer") and **bulk-resolve** on the Data Validation tab's issue list. Backend: `POST /api/followups/bulk` and `PATCH .../validation-issues/bulk-resolve`, both partial-success rather than all-or-nothing — a follow-up bulk request skips (with a per-student reason) anyone not found in the caller's institution or flagged do-not-contact rather than failing the whole batch, and bulk-resolve silently excludes ids that don't match the period or are already resolved, reporting back only the count that actually changed. Both use `createMany`/`updateMany`, which the audit-log Prisma extension doesn't instrument per-row (same documented scope boundary as merge.ts's child-record reassignment). Frontend: row checkboxes + a header "select all" (correctly indeterminate when partially selected) on both the Follow-Up Queue and the Data Validation tab, with a contextual bulk-action button that only appears once something's selected. 8 new integration tests (partial-success skip reasons, cross-period/already-resolved exclusion, role gating) — 137/137 server tests passing. Verified in a real browser: bulk-logged a follow-up across 3 non-contiguous students and confirmed their Attempts/Last Contact/Last Outcome columns updated live and correctly (3→4, 3→4, 0→1); bulk-resolved 3 validation issues and watched the error badge and row list update live; confirmed the pre-existing individual Resolve and Merge actions still work unaffected by the new bulk controls. No bugs found.
- [x] **P12. Bulk student import + saved mapping profiles** — the `imports` module was a genuinely empty placeholder; built the whole pipeline from scratch, not just the mapping-profile layer the spec's framing suggested. Scoped to Student's own core fields (Internal ID, First/Last/Preferred Name, Email, Phone) — enrollments/outcomes need too much institution-specific context (program, campus, reporting period) to map generically from an arbitrary source system's export. Schema gap found and fixed first: `ImportBatch` had no `institutionId` at all, only reachable indirectly through an *optional* `mappingProfileId` — meaning a batch could exist with no institution scoping whatsoever, a real multi-tenancy hole in a table nothing had ever used yet. Added it directly (table was confirmed empty, so a straight `NOT NULL` column was safe) via the same hand-authored-migration process P9's survey tokens needed, plus `originalFilename`/`fileReference`/`columnMapping`/`totalRows`/`importedRowCount` to round out what the batch needs to track across a multi-step wizard. A batch's CSV is stored via a new `importStorage` adapter instance (same `StorageAdapter` interface evidence uploads use, separate directory) and re-parsed at each step rather than persisting parsed rows in the database. Flow: upload -> map columns (with save-as-reusable-profile) -> validate (flags missing required fields, in-file duplicate IDs, and IDs already in the database, via `ImportRowError` rows) -> paginated preview of the valid rows only -> commit (creates only the rows that passed validation, `skipDuplicates: true` as a second line of defense against a same-ID row created by something else between validate and commit). Security finding while adding `csv-parse` as a new dependency: `npm audit` flagged a real, applicable advisory (GHSA-8cw4-87c7-c6xx, prototype pollution reachable through the `columns` auto-mapping option) — not a theoretical one, since CSV header names are exactly the kind of untrusted, attacker-controlled string that option turns into object keys. Fixed two ways at once: upgraded to the patched major version, and designed the parser to never use the `columns` option at all — rows come back as plain `string[][]` and get mapped by validated integer offset instead, sidestepping the whole vulnerability class rather than just outrunning one advisory. Frontend: a "Bulk Import" nav item, a batch-history list page, and a wizard detail page whose content follows the batch's own status (UPLOADED -> MAPPED -> VALIDATED -> PREVIEWED -> IMPORTED), reusing the exact enum already modeled for this in the schema. 16 new integration tests, including one that actually simulates the validate/commit race condition (inserts a colliding student between the two calls) to prove `skipDuplicates` really does prevent a crash rather than just assuming it — 153/153 server tests passing. Verified in a real browser end-to-end across two full upload cycles: deliberately-crafted CSVs with valid rows, a blank-required-field row, and an in-file duplicate ID all produced exactly the expected valid/error counts and reasons; committed students were confirmed to actually exist in the real Students list search (1,199 -> 1,227 after one run) with the bad row correctly excluded; and the second upload under the same source-system name correctly offered and applied the saved mapping profile from the first. No bugs found.
- [x] **P13. Unified Student Communication Timeline** — `CommunicationEvent` was modeled but nothing wrote to it yet. Real design gap fixed first: the schema's single `SURVEY_SENT`/`SURVEY_RESPONSE` pair can't actually tell which table a `sourceId` points into — `GraduateSurvey` and `EmployerSurvey` (and their Response tables) are separate auto-incrementing primary keys, so id 42 could exist in either — split into `GRADUATE_SURVEY_SENT`/`EMPLOYER_SURVEY_SENT`/`GRADUATE_SURVEY_RESPONSE`/`EMPLOYER_SURVEY_RESPONSE` via a hand-authored enum migration (table confirmed empty, so no data to reconcile). Wired writes into every source that actually has a real creation pathway today: `FollowUpAttempt` (both the single and P11's bulk create — bulk create switched from `createMany` to individual `create` calls specifically so each row has a real id to hang an event off of, since `createMany` only returns a count) and all four survey send/response actions. Deliberately left `VERIFICATION_CONTACT` and `NOTIFICATION` unwired — nothing creates a `VerificationRecord` or `Notification` anywhere in the app yet (no endpoint exists for either), so there's nothing real to record; wiring those is naturally scoped to whichever future work actually builds those creation paths. One explicit scope boundary: this only records new activity going forward — the ~824 seeded demo follow-up attempts and a handful of survey rows from earlier testing sessions predate the wiring and won't retroactively appear, since backfilling would have meant either a risky full database reseed (discarding all the manual verification data accumulated across P9-P12) or a separate one-off script; deferred rather than silently attempted. Frontend: a "Timeline" tab on the student detail page rendering a Mantine `Timeline` component, one entry per event with an icon/color per type, its stored summary, and timestamp. 6 new integration tests (per-source event creation, correct chronological ordering across mixed event types, institution scoping) — 159/159 server tests passing. Browser verification surfaced two real bugs, both fixed: (1) the new Timeline tab wasn't invalidated when a follow-up or survey was logged elsewhere on the same page, requiring a manual reload to see new entries — added the missing query invalidations; (2) a genuine pre-existing timezone bug in the follow-up "Record Attempt" datetime picker (both the single form and P11's bulk modal) — `new Date().toISOString().slice(0, 16)` was used as a `datetime-local` input's default value, but that input reads/writes local wall-clock time with no timezone marker while `.toISOString()` is UTC, so the picker defaulted to a time offset by the browser's UTC offset (6 hours ahead for MDT) and, if submitted unchanged, persisted a double-shifted timestamp — visibly broke the Timeline's chronological ordering guarantee for anyone using the default time. Fixed with a `toDatetimeLocalValue()` helper that builds the string from the `Date`'s own local getters instead. Re-verified in a real browser: input now matches actual local time to the minute, the round-tripped stored/displayed timestamp matches real time exactly, and both the Timeline tab and Follow-Up Queue's bulk modal update live without a page reload.
- [x] **Outcomes follow-up deadline** — per-reporting-period deadline distinct from the period's own end date, with a dashboard countdown/reminder (modeled on the training deck's "outcomes due Dec 1, annual report due first week of December" workflow). New nullable `ReportingPeriod.outcomesDeadline` column, plus a narrow `PATCH /reporting-periods/:id` (only this field is editable post-creation — label/dates/ruleSet edits stayed out of scope) gated the same as finalize/submit/reopen. `readiness.ts` computes `daysUntilOutcomesDeadline` server-side so it can never drift from what's displayed. Frontend: an optional field on the New Reporting Period form, plus a color-coded countdown banner (blue >30 days, yellow ≤30, orange ≤7, red once overdue) on the Readiness tab — shared by both the per-period detail page and the Dashboard's Program Health tab, since they reuse the same component — with an inline edit control visible only to System/Institutional Administrators. 11 new integration tests (including the exact day-count math and role gating) — 163/163 server tests passing. Browser verification found and fixed a real bug: the banner displayed the deadline one calendar day earlier than what was actually stored, because a date-only value (stored as UTC midnight) was rendered via `new Date(iso).toLocaleDateString()`, which converts to the viewer's local timezone first — anyone west of UTC loses a day. Fixed with a `formatDateOnly()` helper that builds the display date from the ISO string's Y-M-D components directly rather than through a timezone-converting `Date`. Also fixed, since it directly caused flaky "which period is current" behavior during that same verification: `GET /reporting-periods` had no tiebreaker beyond `startDate desc`, so two periods sharing a start date sorted unstably — added `id desc` as a secondary sort key. Note: the same date-only-via-`toLocaleDateString()` display bug this fixed in the Readiness banner exists in at least 8 other places across the app (reporting period start/end dates, licensure dates, employment dates, etc.) — left alone as a separate, pre-existing, broader cleanup rather than folded into this item's scope. Verification also surfaced (and, per the user's explicit decision, this fixed) an unrelated pre-existing permissions mismatch: "New Reporting Period"/"New COE Rule Set" were shown to any admin role but silently 403'd for anyone but System Administrator — loosened framework/rule-set/reporting-period creation to the same `CAN_FINALIZE` set (System + Institutional Administrator) already used for finalize/submit/reopen, with a new regression test confirming an Institutional Administrator can create all three end-to-end.
- [x] **Missing-outcomes digest** — a pushed summary to staff, vs. today's pull-based Validation tab. `notifications` was a genuinely empty placeholder — nothing anywhere in the app created or read a `Notification` row before this — so the whole per-user inbox was built from scratch, not just this one digest type. Real schema gap fixed first: `Notification` had no field to hold human-readable content at all (just `type` + a polymorphic entity reference), which works for "go look at record #42" but not for a digest, which is a summary/count with nothing single to point at — added a `message String` column, same reasoning as `CommunicationEvent.summaryText` (P13). Also added the `MISSING_OUTCOMES_DIGEST` enum value, distinct from the existing generic `VALIDATION_ERROR`. The digest reuses `getUnresolvedOutcomeStudentIds()` — the same helper the graduate outreach campaign (the other deferred item) already calls — so the report, the campaign, and now the digest all agree on exactly one definition of "unresolved outcome"; sends nothing at all when that count is zero rather than pushing an empty "all clear" notification. Manually triggered, same honesty as the campaign: no job-scheduler infrastructure exists anywhere in this app. Backend: `GET /api/notifications` (per-user inbox + unread count), `PATCH .../:id/read`, `PATCH .../read-all`, `POST .../missing-outcomes-digest` (gated to every operational role — this app's broadest action-permission set — though the only UI path to it currently sits on an admin-only dashboard, see below). Frontend: a notification bell in the header (unread-count badge, dropdown, click-to-mark-read, mark-all-read; polls every 60s — no websocket/SSE infrastructure exists to push through) visible on every page for every role, plus a "Send Missing-Outcomes Digest" button on the Dashboard's Data Quality tab — the P6 "natural home" this item's own description named. 8 new integration tests — 177/177 server tests passing. Browser verification found and fixed two real bugs: (1) sending a digest didn't invalidate the sender's own notification query, so their bell showed stale data until the next 60s poll — added the missing `onSuccess` invalidation; (2) the digest button's own role check listed Program Administrator/Career Services/Instructor Staff, but the Data Quality tab itself is gated to System/Institutional Administrator only (a deliberate, already-verified P6 decision) — those roles could never reach the button at all, dead code. Narrowed the button's gate to match the tab's actual admin-only audience rather than widening the tab itself, since the P6 gate was a considered prior decision, not an oversight. Re-verified both fixes in a real browser with no regressions.
- [x] **Quarterly graduate outreach campaign** — a batched mode for P9's survey system, targeting completers with no resolved outcome yet. Deliberately targets exactly the population P8's Unknown Outcomes report already shows (SEEKING_OR_UNKNOWN classification or no outcome record at all) via a new exported `getUnresolvedOutcomeStudentIds()` helper both the report and the campaign call, so the campaign can never target a different population than what staff can already see and audit. A student appearing in both underlying source queries (a null-outcome student generates both a SEEKING_OR_UNKNOWN classification and a MISSING_OUTCOME_RECORD issue) is deduplicated to exactly one survey. "Quarterly" is a manually-triggered action rather than an actually time-scheduled job — this app has no job-scheduler infrastructure anywhere (every other batch action, like Run Validation, is likewise a button click), so building one would be a much larger, separately-scoped decision; documented plainly in the confirmation modal's own copy rather than pretending otherwise. Skips anyone who already has a still-unanswered graduate survey rather than sending a duplicate, and records a `GRADUATE_SURVEY_SENT` timeline event for each one sent, reusing P13's `recordCommunicationEvent()` — the two deferred items compose naturally since P13 landed first. Lives as a "Start Outreach Campaign" button directly on the Unknown Outcomes report (Reports tab), gated to every role except Read-Only Auditor. 5 new integration tests, including one that specifically proves the both-queries dedup case produces exactly one survey — 169/169 server tests passing. Verified in a real browser: sent to 36 deduplicated students (from 31+10 raw, confirming the 5-student overlap), confirmed a fresh survey + matching Timeline entry on a targeted student, re-ran the campaign immediately and confirmed all 36 were correctly skipped as already-pending rather than duplicated, and confirmed Read-Only Auditor never sees the button. No bugs found.

## Phase 3

Per spec §64. Unlike Phase 2's P1-P13 (bounded extensions of things that already existed), these are largely independent, substantial initiatives — picked one at a time by explicit user choice rather than assumed order.

- [x] **Skills-gap analysis** — compares employers' structured 1-5 skill ratings (Technical Preparedness, Communication, Problem Solving, Professionalism — from P9's Employer Survey) per program against the institution-wide average for each dimension, surfacing which programs employers rate notably below average on which skills, plus the raw free-text `skillsGapNotes` comments for qualitative context. Deliberately not scoped to a reporting period — `EmployerSurvey` has no reporting-period link of its own in the data model, the exact same gap `placementQuality()`/`followUpEffectiveness()` (P8) already document, so this is an all-time view by the same established precedent. A response is attributed to whichever of the graduate's completed enrollments finished most recently, since an employer survey concerns one specific graduate, not a specific program enrollment — there's no stronger link available, and a student with no completed enrollment on file still counts in the institution-wide averages but can't be attributed to a program row. Lives as a 6th sub-tab ("Skills Gap Analysis") on the existing Reports tab (P8), reusing the same components/patterns. 6 new integration tests (including the exact gap arithmetic and program-attribution logic) — 183/183 server tests passing. Verified in a real browser: institution averages and per-program gaps computed correctly, worst-gap program sorted first, expandable employer comments worked, and the report showed identical data across two different reporting periods' Reports tabs, confirming it's genuinely not period-scoped. No bugs found.
- [ ] **Geographic placement reporting** (Leaflet) — needs employer/student location geocoding data the seed doesn't currently produce.
- [x] **Custom Report Builder** — scoped broader than the other Phase 3 items so far, per explicit user choice: covers three entity types (Students, Employers, Programs), not just one. A curated, structured query builder rather than a generic SQL/query-string interface — shared field/filter registries (`shared/src/reportBuilder.ts`) are the entire vocabulary the backend accepts, so there's no injection surface and no way to reference anything not explicitly wired up; safe by construction, not by sanitization. Filters the database can push down (program/campus/enrollment status, employer industry/state/active, program campus/credential/licensure) become Prisma `where` clauses; `employmentStatus` can't be (it depends on a reporting-period-scoped join with at most one row per enrollment per period), so every fetch function returns its FULL filtered row set with no database-level cap, and only the caller (preview vs. export) decides how much to send back — capping at the database layer would have made `totalCount` and the JS-side filter silently disagree. New `SavedReport` model (institution-wide, not creator-restricted, same reasoning as `ImportMappingProfile`) lets a definition be named and reused; running a saved report re-validates against the live registry rather than trusting what was true when it was saved, so a stale saved report fails with a clear error instead of silently returning wrong data if the registry ever changes. Backend: `POST /api/reports/custom/{run,export}`, full saved-report CRUD (creation/deletion gated to every operational role, matching the same read-only-auditor boundary used everywhere else). Frontend: a new top-level "Report Builder" nav item — entity picker, a dynamic filter panel and column checklist that both swap per entity, a results table, Excel export, and save/load/delete for saved reports. 16 new integration tests (all three entities' field selection and filtering, the export producing a real readable `.xlsx`, and the full saved-report lifecycle) — 199/199 server tests passing. Verified in a real browser with values cross-checked directly against the database for all three entities (a 152-student program filter, a 4-placement employer, a 41.67% program completion rate — all exact matches), Excel export, and the full save/load/delete cycle. One real bug found and fixed: switching the "Report on" entity reset the selected columns and filters but not the selected reporting period, so a period chosen for one entity would silently carry over and satisfy another entity's period-gating without the user ever confirming it should apply there too.
  - **Extension: multi-period comparison** — immediately after the above shipped, the user asked to compare data across past years/periods to see trends, not just run against one at a time. `ReportDefinition.reportingPeriodId` (singular) became `reportingPeriodIds?: number[]` (capped at `REPORT_BUILDER_MAX_PERIODS = 10`); with 2+ periods selected, each fetcher now runs once per period and results are concatenated (one row per entity per period), with a `reportingPeriodLabel` column auto-prepended (both in the on-screen table and the Excel export) whenever 2+ periods are involved — 0 or 1 period behaves exactly as before, so this is purely additive. Also made Employer's `placementCount`/`averageWage`/`fullTimeRate` genuinely period-aware for the first time: previously always an all-time aggregate regardless of any period selection, they now scope to `EmploymentRecord`s whose `startDate` falls within each selected period's date range — the same date-range attribution `placementQuality()` (P8) already established, since `EmploymentRecord` still has no `reportingPeriodId` of its own — falling back to the existing all-time total when no period is selected, so this is an enhancement, not a breaking change. Frontend: the single-period `Select` became a `MultiSelect` (searchable, capped at the same max), and the results table shows the new "Reporting Period" column only when the actual response rows contain it (derived from the data itself, not the current picker state, so it stays correct if the picker changes after a report has run). Saved reports round-trip the new array field automatically since `savedReports.ts` just validates against the same shared schema. 7 new integration tests added (single-period-unchanged, multi-period concatenation + labeling, Employer period-scoped stats differing correctly per period vs. all-time with none selected, unknown-period-id rejection, over-the-cap rejection, and the export's period-label column) — 206/206 server tests passing. Two pre-existing all-time Employer fixture-count tests needed updating after adding a second period's placement fixture data (2→3 records, so placement/wage/rate expectations shifted accordingly) — not a regression, just the new fixture data was global to the suite. Verified in a real browser across all three entity types with 2+ periods selected, plus the save/load round-trip and a direct database cross-check confirming the Employer period-scoping is real (not just cosmetic labeling).
  - **Extension: customizable chart panel** — the user then asked for "a histogram comparison of years" on top of the multi-period table; asked what exactly should be charted and the user said to make it customizable rather than picking one fixed chart for them. Entirely client-side (no backend/schema changes) — the multi-period result rows already carry everything needed. New optional `ReportFieldDef.chartKind` (`"numeric-sum" | "numeric-average" | "categorical"`) added per-field to the shared registries, driving which chart modes are even offered based on which columns are currently checked: **Trend across periods** (one bar per period, the chosen numeric column summed or averaged across matching rows — sum for count-like fields like Placement Count, average for rates/percentages, matching each field's own real-world semantics), **Compare by column, per period** (one cluster per distinct value of a user-chosen "Group by" column — e.g. per program — with one bar per period inside each cluster), and **Category counts per period** (a true frequency histogram: row counts per category value, e.g. Enrollment Status or Industry, broken out by period). Only appears at all with 2+ periods selected and a report already run; falls back to a plain hint if none of the checked columns are chart-capable; caps grouped/category charts at 20 bars with a note if there'd be more; and warns when the underlying result was preview-truncated (>500 rows) that the chart reflects only the shown subset. Uses `@mantine/charts`' `BarChart`, already a dependency from P3/P6. Verified in a real browser across Program and Employer entities, all 3 modes, with every rendered bar/tooltip value cross-checked against either the results table or a raw summed database query — exact matches throughout (e.g. Employer Placement Count trend: 219/228 summed across 264 rows per period, matching a server-side sum of the raw API response). One environment gotcha surfaced during verification, not a code bug: an already-running client dev server doesn't pick up a `shared/` rebuild's new fields via Vite's dependency pre-bundle cache — needs a dev-server restart (its existing `predev` cache-clear script then applies), not just a page refresh, the same class of issue `predev` was originally added for.
  - **Extension: line-chart option** — the user then asked to let the chart panel render as a line chart too, not just bars. Added a "Chart style" (Bar/Line) `Select` next to the existing "Chart type" control, defaulting to Bar; purely a rendering swap between Mantine's `BarChart` and `LineChart` using the exact same computed `chartData`/`dataKey`/`series` for whichever chart-type mode is active, so it required no changes to the aggregation logic at all — both components share an identical data-shape contract. Verified in a real browser across all 3 chart-type modes (Trend/Compare-by-column/Category-counts), confirming the correct number of rendered line/bar elements at each toggle and clean switching back and forth with no console errors.
- [ ] **Scheduled reports** — depends on the same job-scheduler infrastructure gap noted throughout the deferred items (P9's campaign, the missing-outcomes digest) — nothing in this app runs on an actual time-based schedule yet.
- [ ] **Advanced workflow automation**
- [ ] **Additional accreditation frameworks beyond COE** — the rule engine (`RuleSet.ruleDefinition`, the classifiers) is currently COE-shaped throughout; generalizing it is a real design project, not a bounded feature.
- [ ] **SIS/API integrations**
  - [ ] **State workforce-agency wage-record match-file import** as a third-party outcome-verification source (the training deck's "DWS/USHE match files reviewed each Fall") — a common, valuable COE compliance workflow, called out explicitly under this item.

## Not yet scheduled (explicitly deferred, not forgotten)

- `RuleSet.rule_definition` internal schema design — blocked on COE documentation review, tracked in `docs/DATA_MODEL.md` §13.
- CI pipeline (GitHub Actions) — add once there's a meaningful test suite to run.
- Deployment target for the public portfolio instance.

## Project review follow-ups (2026-09-18)

Prioritize access control and reliability before further reporting features. This backlog consolidates the review findings, including the earlier registration and access-scoping follow-ups. CI and scheduling below expand the existing deferred items.

### Issues to fix

- [ ] **Critical: restrict public registration.** The unauthenticated route accepts an existing institution ID and any role, including `SYSTEM_ADMINISTRATOR`. Restrict account creation to administrators and handle initial setup separately. See [auth.routes.ts](../server/src/modules/auth/auth.routes.ts) and [auth.service.ts](../server/src/modules/auth/auth.service.ts).
- [ ] **High: enforce program/campus access throughout the API.** Existing scope middleware has no route callers; institution ownership alone does not enforce assigned programs/campuses. Apply shared scopes to detail routes, mutations, lists, search, exports, and reports, with tests for access outside assigned scopes. See [auth.ts](../server/src/middleware/auth.ts) and [students.routes.ts](../server/src/modules/students/students.routes.ts).
- [ ] **High: clear cached data when authenticated identity changes.** Logout leaves the shared React Query cache intact, and query keys lack user/institution identity, allowing a subsequent account in the same tab to briefly see previous-account data. Cancel in-flight requests and clear the cache on identity changes; verify account switching. See [AuthProvider.tsx](../client/src/auth/AuthProvider.tsx).
- [ ] **High: fix the client typecheck/build failure.** The Mantine `Title` link passes an unsupported `to` prop. Correct the polymorphic link rendering and verify client typechecking and build. See [AppLayout.tsx](../client/src/layout/AppLayout.tsx).
- [ ] **Medium: refresh expired access tokens during an active session.** Refresh currently runs only on mount; after token expiry, requests fail while the UI remains authenticated. Add coordinated refresh-and-retry handling for API requests and downloads, and end the session if refresh fails. See [apiClient.ts](../client/src/lib/apiClient.ts).
- [ ] **Medium: validate report filters against their field definitions.** The generic value union accepts mismatched types, leading to downstream errors or silently omitted filters. Validate each field's expected value type, enum values, and IDs; reject duplicate filters and test malformed definitions. See [customReportBuilder.ts](../server/src/modules/reports/customReportBuilder.ts) and [reportBuilder.ts](../shared/src/reportBuilder.ts).
- [ ] **Medium: audit upsert operations.** The audit extension skips `upsert`, including communication preference and saved-report writes. Add explicit auditing or transaction-aware extension coverage so consent/do-not-contact changes and saved-report changes have reliable history. See [prisma.ts](../server/src/lib/prisma.ts), [communicationPreference.ts](../server/src/modules/students/communicationPreference.ts), and [savedReports.ts](../server/src/modules/reports/savedReports.ts).

### Features and infrastructure

- [ ] **User administration:** invitations, account deactivation, password reset, and program/campus assignment, supporting the registration and access-control fixes above. The current [users module](../server/src/modules/users/users.ts) provides only a staff directory.
- [ ] **Report provenance:** include selected reporting periods, filters, generation time, and calculation freshness in exports. Clearly distinguish current enrollment attributes from historical outcomes in multi-period reports.
- [ ] **Report pagination and bounded exports:** avoid fetching every matching row before truncating previews. Add database pagination and queued exports for large reports while preserving accurate filtering and total counts.
- [ ] **CI checks:** add GitHub Actions for typechecking, unit tests, and integration tests against an isolated database. This advances the deferred CI item now that a meaningful test suite exists.
- [ ] **Reusable scheduled-job infrastructure:** support retries and delivery history for outreach campaigns, missing-outcome reminders, and the scheduled reports already listed in Phase 3.

Review validation: server/shared typechecks and all 27 server unit tests passed. Client typechecking failed as noted above. Client tests were blocked by sandbox `EPERM`, and the elevated rerun was declined. Database integration tests and browser flows were not run during this review.
